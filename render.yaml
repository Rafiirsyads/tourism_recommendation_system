services:
  - type: web
    name: tourism-recommendation
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run tourism_ui.py --server.port=$PORT --server.address=0.0.0.0
    envVars:
      - key: PORT
        value: 10000